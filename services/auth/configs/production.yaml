app:
  name: tiki-auth
  env: production
  log_level: info

password:
  algorithm: argon2id
  argon2_memory: 65536
  argon2_time: 3
  argon2_threads: 4
  argon2_key_len: 32

rate_limit:
  login_max_attempts: 5
  login_window: 5m
  account_lockout: 10
  lockout_duration: 30m
