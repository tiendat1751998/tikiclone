apiVersion: apps/v1
kind: Deployment
metadata:
  name: tiki-api-gateway
  labels:
    app: tiki-api-gateway
    version: v1
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tiki-api-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: tiki-api-gateway
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8109"
    spec:
      serviceAccountName: tiki-api-gateway
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: tiki-api-gateway
          image: tiki-clone/tiki-api-gateway:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8109
            - name: grpc
              containerPort: 9109
          envFrom:
            - configMapRef:
                name: tiki-api-gateway-config
            - secretRef:
                name: tiki-api-gateway-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
