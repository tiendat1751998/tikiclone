apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tiki-api-gateway
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: tiki-api-gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
