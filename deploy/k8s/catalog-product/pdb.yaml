apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: catalog-product
  namespace: tiki
  labels:
    app: catalog-product
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: catalog-product
